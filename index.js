//Fazendo import da classe CLI
const CLI = require('./CLI');

//Instanciando classe CLI
const cli = new CLI();

//Função iniciar() é responsável por apresentar as mensagens de inicialização (layout padrão)
cli.iniciar();
